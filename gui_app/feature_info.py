"""
Описания признаков для всплывающих подсказок
"""
FEATURE_INFO = {
    'is_multimodal': {
        'title': 'Мультимодальность',
        'short': 'Много локальных минимумов?',
        'full': (
            '1 = да — ландшафт с множеством впадин и пиков.\n'
            '    Тригонометрические функции (sin, cos) от x\n'
            '    создают волнообразный рельеф с ловушками.\n'
            '    Примеры: Rastrigin, Ackley, Griewank\n\n'
            '0 = нет — гладкий ландшафт, одна «воронка».\n'
            '    Оптимизатор не застрянет в ложном минимуме.\n'
            '    Примеры: Sphere, Rosenbrock, Ellipsoid\n\n'
            'Как определяется:\n'
            'Ищем sin/cos, аргумент которых зависит от x.\n'
            'Если такие есть — функция мультимодальна.'
        ),
    },
    'is_periodic': {
        'title': 'Периодичность',
        'short': 'Повторяющийся рельеф?',
        'full': (
            '1 = да — ловушки повторяются равномерно.\n'
            '    sin/cos с линейным аргументом (a·x + b)\n'
            '    создают одинаковые впадины на равных\n'
            '    расстояниях по всему пространству.\n'
            '    Примеры: Rastrigin cos(2πx), Eggcrate sin(x)\n\n'
            '0 = нет — либо тригонометрии нет, либо:\n'
            '    • аргумент нелинейный (sin(√x), cos(x²)) —\n'
            '      осцилляции сгущаются или растягиваются;\n'
            '    • результат подавлен exp(...) —\n'
            '      осцилляции затухают вдали от центра.\n'
            '    Примеры: Ackley (cos внутри exp → затухание),\n'
            '             Salomon (cos(√(Σx²)) → нелинейный)\n\n'
            'Как определяется:\n'
            '• Аргумент линеен = нет x², |x|, √x внутри.\n'
            '• Не обёрнут в exp = осцилляции не затухают.\n'
            '  (abs-обёртка не считается — она лишь\n'
            '   отражает, но не подавляет периодичность.)'
        ),
    },
    'is_separable': {
        'title': 'Сепарабельность',
        'short': 'Переменные независимы?',
        'full': (
            '1 = да — f(x) = Σ gᵢ(xᵢ), каждую переменную\n'
            '    можно оптимизировать отдельно.\n'
            '    Примеры: Sphere, Rastrigin, Eggcrate\n\n'
            '0 = нет — переменные связаны, нельзя\n'
            '    оптимизировать покоординатно.\n'
            '    Примеры: Rosenbrock, Ackley, Griewank\n\n'
            'Связь возникает, когда:\n'
            '• Соседние переменные в одном выражении:\n'
            '  x[i] и x[i+1] вместе (Rosenbrock, Bohachevsky)\n'
            '• Все компоненты перемножены:\n'
            '  prod(f(xᵢ)) — изменение одной xᵢ влияет\n'
            '  на вклад всех остальных (Griewank)\n'
            '• Нелинейная свёртка суммы:\n'
            '  exp(Σ...) или (Σ...)^k — сумма возведена\n'
            '  в степень или пропущена через exp,\n'
            '  что создаёт перекрёстные зависимости\n'
            '  (Ackley, Salomon, Zakharov, Happy Cat)'
        ),
    },
    'has_product_terms': {
        'title': 'Произведения',
        'short': 'Есть произведения разных переменных?',
        'full': (
            '1 = да — в формуле есть мультипликативные связи между РАЗНЫМИ переменными:\n\n'
            '    • Явное произведение разных переменных: x[i] * x[j] (i ≠ j)\n'
            '      или x[i] * sin(x[j]) при i ≠ j\n\n'
            '    • prod(trig(x)) — все косинусы перемножены\n'
            '      Griewank: ∏ cos(xᵢ/√i)\n\n'
            '    • (выражение с разными xᵢ)^k — при раскрытии появляются xᵢ·xⱼ\n'
            '      Rosenbrock: (x[i+1] − x[i]²)²\n'
            '      Dixon-Price: (2x[i+1]² − x[i])²\n\n'
            '    • (Σ aᵢxᵢ)^k — сумма в степени\n'
            '      Zakharov: (Σ 0.5·i·xᵢ)²\n'
            '      Goodwin: (Σ xᵢ)²\n\n'
            '0 = нет — только произведения в пределах одной переменной или суммы:\n'
            '    • x[i] * sin(x[i]) — Alpine N1, Michalewicz, Damped Schwefel\n'
            '    • (x[i]−1)² · (1 + 10·sin²(...)) — Levy (все множители от одной переменной)\n'
            '    • Sphere: Σ xᵢ²,  Rastrigin: Σ(xᵢ² − cos)\n'
            '    • Eggcrate: Σ(xᵢ² + sin²(xᵢ))'
        ),
    },
    'trig_nesting_depth': {
        'title': 'Вложенность знач. ф.',
        'short': 'Глубина вложеннности цепочки функций',
        'full': (
            'Сколько значимых функций (sin, cos, exp, abs)\n'
            'вложены друг в друга. Чем глубже — тем сложнее\n'
            'рельеф и труднее оптимизация.\n\n'
            '0 = нет тригонометрии\n'
            '    Sphere, Rosenbrock, Qing\n\n'
            '1 = один уровень — простая осцилляция:\n'
            '    cos(2πx)        — Rastrigin\n'
            '    cos(x/√i)       — Griewank\n'
            '    sin(π·w)        — Levy\n\n'
            '2 = два уровня — функция внутри функции:\n'
            '    exp(…cos(2πx)…) — Ackley (exp → cos)\n'
            '    sin(|x|^0.5)    — Schwefel (sin → abs)\n'
            '    abs(x·sin(x))   — Alpine1 (abs → sin)\n\n'
            '3 = три уровня:\n'
            '    exp(…sin(|x|^0.5)…) — Xin-She-Yang\n'
            '    (exp → sin → abs)\n\n'
            'Учитываются только функции, аргумент которых\n'
            'зависит от x. Функции от констант (например,\n'
            '√(i+1) в Griewank) не увеличивают глубину.'
        ),
    },
    'trig_frequency': {
        'title': 'Частота триг.',
        'short': 'Коэффициент при x в sin/cos',
        'full': (
            'Числовой множитель перед x в аргументе\n'
            'тригонометрии. Чем больше — тем чаще\n'
            'осцилляции и больше ловушек на единицу длины.\n\n'
            ' 0.0  — нет тригонометрии\n'
            '         Sphere, Rosenbrock, Qing\n\n'
            ' 1.0  — низкая частота: sin(x), cos(x/√i)\n'
            '         Griewank, Michalewicz, Eggcrate,\n'
            '         Alpine1, Salomon, Schwefel\n\n'
            ' 1.57 — средняя: sin(2π · (x/4 + ¾))\n'
            '         реальный коэфф = 2π/4 = π/2 ≈ 1.57\n'
            '         Levy\n\n'
            ' 6.28 — высокая: cos(2πx), коэфф = 2π\n'
            '         Rastrigin, Ackley, Goodwin\n\n'
            '12.57 — очень высокая: cos(4πx), коэфф = 4π\n'
            '         Bohachevsky\n\n'
            'Для нелинейных аргументов (sin(√x), cos(x²))\n'
            'частота переменная, поэтому ставится 1.0.\n\n'
            'Если в формуле есть подстановка w = a·x + b,\n'
            'извлекается итоговый коэффициент a.\n'
            'Из всех trig-вызовов берётся максимальный.'
        ),
    },
}